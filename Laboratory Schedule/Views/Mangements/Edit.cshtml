@model int

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Management</h4>
<hr />
<div class="row">
    
        <form asp-action="Edit">
            <div class="row mt-3">
            <div class="col-2 mx-2 my-2">
                    <select class="form-select float-start">
                        <option>Language</option>
                        <option value="1">English </option>
                        <option value="2">Arabic </option>
                    </select>
                </div>
            </div>
            <div class="row bg-info mx-2 rounded ">
                <h5 class=" text-light text-center">System Configuration</h5>
            </div>
            <div class=" = row">

            <div class="col-4 mx-2 my-2">
                  <label  class="control-label"> Limitation Days</label>
                  <input name="limitationDays" class="form-control" value="@Model" />
               </div>
              
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary my-2 mx-2" />
            </div>
            </div>
        </form>
   
</div>

<div>
    <a asp-action="Index" asp-controller="Requestes" >Back to Request List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}